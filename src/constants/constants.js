import { RoutePath } from './routeConstants';

const reduxResources = {
  ALERT: 'alert',
  LOADING: 'loading',
  COUNTER: 'counter',
  LANGUAGE: 'language',
};

const languageConstants = {
  VI: 'vi',
  EN: 'en',
};

export { RoutePath, reduxResources, languageConstants };
